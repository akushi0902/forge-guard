/**
 * Security Review placeholder page — to be implemented in later WOs.
 */
import { type JSX } from 'react';
import { Stack, Text, Title } from '@mantine/core';

export function SecurityPage(): JSX.Element {
  return (
    <Stack gap="md">
      <Title order={2}>Security Review</Title>
      <Text c="dimmed">Security finding escalations — coming soon.</Text>
    </Stack>
  );
}
