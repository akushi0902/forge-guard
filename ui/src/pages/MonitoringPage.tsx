/**
 * Monitoring placeholder page — to be implemented in later WOs.
 */
import { type JSX } from 'react';
import { Stack, Text, Title } from '@mantine/core';

export function MonitoringPage(): JSX.Element {
  return (
    <Stack gap="md">
      <Title order={2}>Monitoring</Title>
      <Text c="dimmed">Operational monitoring — coming soon.</Text>
    </Stack>
  );
}
