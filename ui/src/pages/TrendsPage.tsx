/**
 * Trends placeholder page — to be implemented in later WOs.
 */
import { type JSX } from 'react';
import { Stack, Text, Title } from '@mantine/core';

export function TrendsPage(): JSX.Element {
  return (
    <Stack gap="md">
      <Title order={2}>Trends</Title>
      <Text c="dimmed">Trend analytics — coming soon.</Text>
    </Stack>
  );
}
