/**
 * AI Agent placeholder page — to be implemented in later WOs.
 */
import { type JSX } from 'react';
import { Stack, Text, Title } from '@mantine/core';

export function AiAgentPage(): JSX.Element {
  return (
    <Stack gap="md">
      <Title order={2}>AI Agent</Title>
      <Text c="dimmed">AI-powered governance assistant — coming soon.</Text>
    </Stack>
  );
}
