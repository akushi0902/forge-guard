/**
 * RBAC placeholder page — to be implemented in later WOs.
 */
import { type JSX } from 'react';
import { Stack, Text, Title } from '@mantine/core';

export function RbacPage(): JSX.Element {
  return (
    <Stack gap="md">
      <Title order={2}>RBAC</Title>
      <Text c="dimmed">Role-based access control — coming soon.</Text>
    </Stack>
  );
}
