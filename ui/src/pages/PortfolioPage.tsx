/**
 * Portfolio placeholder page — to be implemented in later WOs.
 */
import { type JSX } from 'react';
import { Stack, Text, Title } from '@mantine/core';

export function PortfolioPage(): JSX.Element {
  return (
    <Stack gap="md">
      <Title order={2}>Portfolio</Title>
      <Text c="dimmed">Engineering portfolio overview — coming soon.</Text>
    </Stack>
  );
}
